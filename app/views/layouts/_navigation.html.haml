= link_to 'Main', :root

- if user_signed_in? && current_user.role?('admin')
  = link_to 'Users', :admin_users
  = link_to 'Black Domains', :admin_black_domains
  = link_to 'Settings', :admin_edit_settings
  = link_to 'Brochures', :admin_brochures

- if user_signed_in?
  = link_to 'Logout', :destroy_user_session

- else
  = link_to 'Sign Up', :new_user_registration
